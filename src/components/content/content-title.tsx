import styled from 'styled-components'

export const ContentTitle = styled.h1`
  font-size: 2rem;
  font-weight: 600;
  letter-spacing: 0.3rem;
  margin-bottom: 3rem;
  white-space: nowrap;

  &:not(:first-child) {
    margin-top: 4rem;
  }
`
